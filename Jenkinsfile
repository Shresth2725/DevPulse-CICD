pipeline {
    agent any

    stages {
        stage('Sanity Check') {
            steps {
                echo 'Jenkins is running CI from DevPulse-CICD'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
